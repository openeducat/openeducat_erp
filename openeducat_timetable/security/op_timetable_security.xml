<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.rule" id="student_timetable_rule">
            <field name="name">Student Timetable rule</field>
            <field name="model_id" ref="model_op_timetable" />
            <field name="groups"
                eval="[(4, ref('openeducat_core.group_op_student'))]" />
            <field name="domain_force"> [('user_ids','in',user.id)]</field>
        </record>

    </data>
</odoo>
